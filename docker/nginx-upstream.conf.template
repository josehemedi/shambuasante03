# Résolution DNS Docker + upstream backend
# BACKEND_UPSTREAM : backend:8082 (réseau partagé) ou host.docker.internal:8082
resolver 127.0.0.11 valid=10s ipv6=off;

upstream backend {
    server ${BACKEND_UPSTREAM};
    keepalive 16;
}
