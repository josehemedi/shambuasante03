# Résolution du backend — surchargeable via env BACKEND_UPSTREAM
# Exemple compose : backend:8082  |  host.docker.internal:8082
resolver 127.0.0.11 valid=10s ipv6=off;

upstream backend {
    server ${BACKEND_UPSTREAM};
}
